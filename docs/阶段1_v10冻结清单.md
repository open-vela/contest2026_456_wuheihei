# 阶段 1：v10 冻结清单

## 1. 项目标识

- 队伍：呜嘿嘿
- 成员：李炳霖、吴安琪
- 官方仓库：https://github.com/open-vela/contest2026_456_wuheihei
- 当前状态：仅形成本地审查包，尚未推送官方仓库

## 2. v10 定义

本次按用户确认的规则，将服务器 `charm` 上修改时间最新的真实开发板镜像及其当前源码、配置定义为 v10。

- 镜像：`/data/home2/bling/project/qrs/openvela-work/openvela/nuttx/nuttx.bin`
- 服务器修改时间：2026-09-18 10:45:09（UTC-04:00）
- 北京时间：2026-09-18 22:45:09（UTC+08:00）
- 文件大小：113,143,844 字节
- SHA256：`046ae62216b8cdc00681471e58f8eb46837a5d4c399e7fd59fe364f3684658c2`

本次没有重新构建或重新部署该镜像。

## 3. 三个运行端的边界

| 运行端 | 作用 | 本次证据 | 可以声明的结果 |
|---|---|---|---|
| Python 训练与离线评测 | 特征缓存、数据增强、五折训练、Float/INT8 评测、噪声鲁棒性 | 重新执行当前 `train_edge_mfcc.py` | 模型交叉验证指标、各类别指标、量化前后差异 |
| C 模拟器推理 | 编译当前 OpenVela C 特征提取、INT8 模型和三秒决策逻辑 | 重新编译并运行测试 WAV | 证明 C 链路可运行及输出行为；不能代替五折准确率 |
| 真实 OpenVela 开发板 | R528S3 板级音频、LCD、触摸、LVGL、自动启动和实时采集 | 冻结当前源码、配置和镜像 | 可以声明已完成代码集成和镜像构建；本次不声明新的真机准确率、延迟或稳定性指标 |

## 4. Python 模型冻结信息

- 当前模型：`tiny_mlp_mfcc92_edge_robust.npz`
- 架构：`92 → 64 → 5`
- 参数量：6,277
- 输入特征：40 个时域统计量 + MFCC 均值/标准差 + MFCC Delta 均值/标准差，共 92 维
- 类别：background、cough、glass_break、baby_cry、dog_bark
- 原部署模型 SHA256：`5382765b0aeb0c0aa9f96f7f78b26728acfcd20efd35ac7b045637889b50df62`
- 本次重新训练模型 SHA256：`5382765b0aeb0c0aa9f96f7f78b26728acfcd20efd35ac7b045637889b50df62`

两个校验值完全一致，说明当前训练流程在固定随机种子和相同缓存输入下可确定性复现部署模型。

## 5. 数据冻结信息

- 评测窗口：1,950 个一秒窗口
- 独立源录音：390 条
- background：230 条源录音
- cough：40 条源录音
- glass_break：40 条源录音
- baby_cry：40 条源录音
- dog_bark：40 条源录音
- 数据划分：使用 ESC-50 官方五折编号，同一源录音的所有窗口保持在同一折

由于 background 数量明显更多，后续报告必须同时给出总体准确率、宏平均和各类别指标，不能只写总体准确率。

## 6. OpenVela 板级冻结信息

当前板级配置确认启用：

- R528 / R528S3 Gemini S1
- R528 Audio
- PCM 音频
- LCD / ILI9341
- 触摸输入
- LVGL
- `CONFIG_AE_AUDIODETECT=y`
- `CONFIG_AE_AUDIODETECT_UI=y`
- `CONFIG_AE_AUDIODETECT_UI_AUTOSTART=y`

启动脚本在硬件服务初始化后等待 3 秒，再执行 `audiodetect ui &`。

## 7. 冻结快照位置

- Python 当前训练代码与模型：`v10_source_snapshot/python_model/`
- OpenVela 当前应用源码：`v10_source_snapshot/openvela_app/audiodetect/`
- 板级配置和启动脚本：`v10_source_snapshot/board/`
- 完整文件清单与 SHA256：`evidence/v10_freeze_manifest.json`

本快照仅用于审查。后续进入官方仓库时会重新组织目录，但不会混入旧赛代码。

注意：冻结源码中的 `apps/audiodetect/README.md` 修改时间早于实时 UI 源码，仍描述为 WAV 文件测试并称实时采集尚待替换，已经与 v10 实现不一致。该 README 仅作为原样快照保留，不能作为功能事实依据；进入阶段 3 时应根据当前源码重写文档，但不改冻结的业务代码。
